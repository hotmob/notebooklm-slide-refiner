# Examples

Generate a sample PDF used by the README:

```bash
python generate_sample_pdf.py
```

The script will download an open-source font (Noto Sans SC) if not present so Chinese text can render consistently.
